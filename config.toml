# The URL the site will be built for
base_url = "https://binderdavid.github.io"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# We use the DeepThought theme
theme = "DeepThought"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
katex.enabled = true
katex.auto_render = true

chart.enabled = true
mermaid.enabled = true
galleria.enabled = true

navbar_items = [
 { code = "en", nav_items = [
  { url = "$BASE_URL/", name = "Home" },
  { url = "$BASE_URL/publications", name = "Publications" },
  { url = "$BASE_URL/teaching", name = "Teaching" },
  { url = "$BASE_URL/talks", name = "Talks" },
  { url = "$BASE_URL/blog", name = "Blog" },
 ]},
]

# Add links to favicon, you can use https://realfavicongenerator.net/ to generate favicon for your site
[extra.favicon]
favicon_16x16 = "/icons/favicon-16x16.png"
favicon_32x32 = "/icons/favicon-32x32.png"
apple_touch_icon = "/icons/apple-touch-icon.png"
safari_pinned_tab = "/icons/safari-pinned-tab.svg"
webmanifest = "/icons/site.webmanifest"

# Author details
[extra.author]
name = "David Binder"
avatar = "/images/avatar.png"

# Social links
[extra.social]
email = "david.binder(at)uni-tuebingen.de"
# facebook = "<facebook_username>"
github = "BinderDavid"
# gitlab = "<gitlab_username>"
# keybase = "<keybase_username>"
# linkedin = "<linkedin_username>"
# stackoverflow = "<stackoverflow_userid>"
# twitter = "<twitter_username>"
# instagram = "<instagram_username>"
# behance = "<behance_username>"
# google_scholar = "<googlescholar_userid>"
orcid = "0000-0003-1272-0972"
mastodon = "davidb"
mastodon_server = "types.pl"


# To add google analytics
[extra.analytics]
# google = "<your_gtag>"

# To add disqus comments
[extra.commenting]
# disqus = "<your_disqus_shortname>"

# To enable mapbox maps
[extra.mapbox]
# enabled = true
# access_token = "<your_access_token>"
